* 1 数组解构实现值得交换

  ```javascript
  let a = 'world',
      b = 'hello';
  [a, b] = [b, a];
  console.log(a); // hello
  console.log(b); // world
  ```

  ​

* 2 数组拼接

  扩展运算符 取代 concat

  ```Javascript
  const one = ['a', 'b', 'c'];
  const two = ['d', 'e', 'f'];

  const result = [...one, ...two];

  ```

  ​

* ​





